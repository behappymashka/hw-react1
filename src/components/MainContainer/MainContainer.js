import React from 'react';
import './MainContainer.css';

function MainContainer() {
    return (
        <main className="main-container">
            <h2>Сentral Container</h2>
            <p>This is the central container</p>
        </main>
    );
}

export default MainContainer;